using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using ETS2.PM.Service.WL.Model;
using ETS.Data.SqlHelper;
using ETS2.PM.Service.WL.Data.Internal;
using ETS2.CRM.Service.CRMService.Modle;

namespace ETS2.PM.Service.WL.Data
{
    public class WlDealResponseData
    {

        //public List<WlDealResponseBody> GetDealResponseBody(out int totalcount, Agent_company agentinfo, string method, List<string> productIdList, int currentPage = 0, int pageSize = 0)
        //{
        //    using(var helper=new SqlHelper())
        //    {
        //        List<WlDealResponseBody> list = new InternalWLDealResponse(helper).GetDealResponseBody(out totalcount, agentinfo, method, productIdList, currentPage, pageSize);
        //        return list;
        //    }
        //}
    }
}
